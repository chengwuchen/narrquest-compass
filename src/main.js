import React from "react";
import ReactDOM from "react-dom/client";
import NarrQuestCompass from "./pages/index.js";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NarrQuestCompass />
  </React.StrictMode>
);
